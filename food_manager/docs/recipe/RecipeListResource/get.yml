---
description: Retrieve the full list of recipes
responses:
  '200':
    description: A JSON array of recipe objects
    content:
      application/json:
        schema:
          type: array
          items:
            $ref: '#/components/schemas/Recipe'
