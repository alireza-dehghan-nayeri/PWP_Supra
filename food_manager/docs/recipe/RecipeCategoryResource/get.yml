---
summary: Retrieve a recipe (including its categories)
parameters:
  - $ref: '#/components/parameters/recipeId'
responses:
  '200':
    description: Recipe with categories
    content:
      application/json:
        schema:
          $ref: '#/components/schemas/Recipe'
  '404':
    description: Recipe not found
